import Foundation

// MARK: - App Store product identifiers (must match App Store Connect exactly)
enum SubscriptionProductID {
    static let basicMonthly = "app.Snorry.Snorry.basic.monthly"
    static let all = [basicMonthly]
}
